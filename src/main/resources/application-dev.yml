spring:
  datasource:
    url: jdbc:mysql://localhost:3306/blog?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf-8&autoReconnect=true&failOverReadOnly=false
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    mvc:
      hiddenmethod:
        filter:
          enabled: true

logging:
  level:
    root: info
    com.secondblog: debug
  file:
    name: log/blog-dev.log




